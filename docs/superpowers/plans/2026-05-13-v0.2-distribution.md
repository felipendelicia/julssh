# julssh v0.2 Distribution Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Configurar pipeline de distribución automática con GoReleaser + GitHub Actions que publique binarios Linux amd64/arm64 y un `.deb` en cada tag `v*.*.*`.

**Architecture:** GoReleaser maneja compilación cross-platform, empaquetado `.deb` y publicación al GitHub Release. GitHub Actions dispara GoReleaser en cada tag. `install.sh` se extiende con `--download` para instalar sin Go. `main.go` recibe `--version` inyectado por GoReleaser.

**Tech Stack:** GoReleaser v2, GitHub Actions, bash, GitHub API (curl)

---

## File Map

| Archivo | Acción | Responsabilidad |
|---------|--------|----------------|
| `.goreleaser.yml` | Crear | Config de builds, archives, .deb, changelog, release |
| `.github/workflows/release.yml` | Crear | Trigger en tag v*.*.*, ejecutar GoReleaser |
| `main.go` | Modificar | Agregar `var version = "dev"` + flag `--version` |
| `install.sh` | Modificar | Agregar modo `--download` via GitHub API |
| `README.md` | Modificar | Actualizar sección instalación con curl oneliner |

---

## Task 1: main.go — variable version y flag --version

**Files:**
- Modify: `main.go`

- [ ] **Step 1: Agregar variable version y flag --version**

Reemplazar el contenido de `main.go` con:

```go
package main

import (
	"fmt"
	"log"
	"os"
	"path/filepath"

	tea "github.com/charmbracelet/bubbletea"
	"github.com/felipem/julssh/internal/model"
	"github.com/felipem/julssh/internal/store"
)

// version is set at build time by GoReleaser via -X main.version={{.Version}}
var version = "dev"

func main() {
	if len(os.Args) > 1 && os.Args[1] == "--version" {
		fmt.Println("julssh", version)
		os.Exit(0)
	}

	configDir, err := os.UserConfigDir()
	if err != nil {
		log.Fatalf("no se puede determinar config dir: %v", err)
	}
	storePath := filepath.Join(configDir, "julssh", "connections.json")

	s, err := store.Load(storePath)
	if err != nil {
		fmt.Fprintf(os.Stderr, "error al cargar conexiones: %v\n", err)
		os.Exit(1)
	}

	app := model.NewApp(s)
	p := tea.NewProgram(app, tea.WithAltScreen())
	if _, err := p.Run(); err != nil {
		fmt.Fprintf(os.Stderr, "error: %v\n", err)
		os.Exit(1)
	}
}
```

- [ ] **Step 2: Build y verificar --version**

```bash
go build -o julssh .
./julssh --version
```

Expected: `julssh dev`

- [ ] **Step 3: Verificar tests siguen pasando**

```bash
go test ./...
```

Expected: ok para store y ssh.

- [ ] **Step 4: Commit**

```bash
git add main.go
git commit -m "feat: add --version flag injected by GoReleaser"
```

---

## Task 2: .goreleaser.yml

**Files:**
- Create: `.goreleaser.yml`

- [ ] **Step 1: Crear .goreleaser.yml**

```yaml
version: 2

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - env: [CGO_ENABLED=0]
    goos: [linux]
    goarchs: [amd64, arm64]
    ldflags: -s -w -X main.version={{.Version}}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"

nfpms:
  - package_name: julssh
    homepage: https://github.com/felipendelicia/julssh
    description: Gestor TUI de conexiones SSH. Dedicado a mi hermana Julieta.
    maintainer: Felipe Ndelicia
    license: MIT
    formats: [deb]
    bindir: /usr/local/bin
    contents:
      - src: README.md
        dst: /usr/share/doc/julssh/README.md

checksum:
  name_template: checksums.txt

release:
  github:
    owner: felipendelicia
    name: julssh
  name_template: "v{{ .Version }}"

changelog:
  sort: asc
  filters:
    exclude: ['^docs:', '^chore:']
```

- [ ] **Step 2: Instalar goreleaser y validar config**

```bash
go install github.com/goreleaser/goreleaser/v2@latest
goreleaser check
```

Expected: `• config is valid` sin errores.

Si goreleaser no está en PATH después de install, agregarlo:
```bash
export PATH="$HOME/go/bin:$PATH"
goreleaser check
```

- [ ] **Step 3: Commit**

```bash
git add .goreleaser.yml
git commit -m "feat: add GoReleaser config — linux amd64/arm64 + deb"
```

---

## Task 3: .github/workflows/release.yml

**Files:**
- Create: `.github/workflows/release.yml`

- [ ] **Step 1: Crear directorio y workflow**

```bash
mkdir -p .github/workflows
```

Crear `.github/workflows/release.yml`:

```yaml
name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - uses: goreleaser/goreleaser-action@v6
        with:
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

- [ ] **Step 2: Verificar YAML válido**

```bash
python3 -c "import yaml, sys; yaml.safe_load(open('.github/workflows/release.yml'))" && echo "YAML OK"
```

Expected: `YAML OK`

- [ ] **Step 3: Commit**

```bash
git add .github/
git commit -m "ci: add GitHub Actions release workflow"
```

---

## Task 4: install.sh — modo --download

**Files:**
- Modify: `install.sh`

- [ ] **Step 1: Reemplazar install.sh con versión que soporta --download**

```bash
#!/usr/bin/env bash
set -e

BINARY="julssh"
DEFAULT_DEST="$HOME/.local/bin"
REPO="felipendelicia/julssh"

# ── args ──────────────────────────────────────────────────────────────────────

DOWNLOAD=false
DEST="$DEFAULT_DEST"

for arg in "$@"; do
    case "$arg" in
        --download) DOWNLOAD=true ;;
        *) DEST="$arg" ;;
    esac
done

# ── checks ────────────────────────────────────────────────────────────────────

if [[ "$DEST" == "/usr/local/bin" && "$EUID" -ne 0 ]]; then
    echo "Error: instalar en /usr/local/bin requiere sudo." >&2
    echo "  Usá: sudo bash install.sh /usr/local/bin" >&2
    exit 1
fi

mkdir -p "$DEST"

# ── install ───────────────────────────────────────────────────────────────────

if $DOWNLOAD; then
    if ! command -v curl &>/dev/null; then
        echo "Error: curl no está instalado." >&2
        exit 1
    fi

    ARCH=$(uname -m)
    case "$ARCH" in
        x86_64)          ARCH="amd64" ;;
        aarch64 | arm64) ARCH="arm64" ;;
        *)
            echo "Error: arquitectura no soportada: $ARCH" >&2
            exit 1
            ;;
    esac

    echo "Obteniendo última versión de $REPO..."
    DOWNLOAD_URL=$(curl -fsSL "https://api.github.com/repos/$REPO/releases/latest" \
        | grep "browser_download_url" \
        | grep "linux_${ARCH}\.tar\.gz" \
        | cut -d '"' -f 4)

    if [[ -z "$DOWNLOAD_URL" ]]; then
        echo "Error: no se encontró binario para linux/$ARCH en el último release." >&2
        exit 1
    fi

    echo "Descargando $DOWNLOAD_URL..."
    TMP=$(mktemp -d)
    trap 'rm -rf "$TMP"' EXIT
    curl -fsSL "$DOWNLOAD_URL" | tar -xz -C "$TMP"
    mv "$TMP/$BINARY" "$DEST/$BINARY"
    chmod +x "$DEST/$BINARY"
else
    if ! command -v go &>/dev/null; then
        echo "Error: Go no está instalado." >&2
        echo "  Instalalo desde https://go.dev/dl/" >&2
        echo "  O usá --download para instalar sin Go." >&2
        exit 1
    fi

    echo "Compilando $BINARY..."
    go build -ldflags="-s -w" -o "$DEST/$BINARY" .
fi

echo "Instalado en $DEST/$BINARY"

# ── PATH warning ──────────────────────────────────────────────────────────────

if ! echo "$PATH" | tr ':' '\n' | grep -qx "$DEST"; then
    echo ""
    echo "Advertencia: $DEST no está en tu \$PATH."
    echo "Agregá esto a tu ~/.bashrc o ~/.zshrc:"
    echo ""
    echo "  export PATH=\"$DEST:\$PATH\""
    echo ""
fi

echo "Listo. Ejecutá: $BINARY"
```

- [ ] **Step 2: Dar permisos y verificar modo compilación (el existente)**

```bash
chmod +x install.sh
bash install.sh
```

Expected: `Compilando julssh... Instalado en /home/<user>/.local/bin/julssh`

- [ ] **Step 3: Verificar que --download falla gracefully si no hay releases aún**

```bash
bash install.sh --download 2>&1 || true
```

Expected: algún error claro (no hay releases aún en el repo, o el release no tiene binarios). No debe crashear con stacktrace.

- [ ] **Step 4: Commit**

```bash
git add install.sh
git commit -m "feat: install.sh — add --download mode via GitHub Releases API"
```

---

## Task 5: README — actualizar sección instalación

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Reemplazar sección Instalación**

Reemplazar la sección `## Instalación` del README con:

```markdown
## Instalación

### Sin Go (recomendado)

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/felipendelicia/julssh/main/install.sh) --download
```

Descarga el binario pre-compilado del último [GitHub Release](https://github.com/felipendelicia/julssh/releases). No requiere Go instalado.

### Desde fuente

Requiere Go 1.21+.

```bash
git clone https://github.com/felipendelicia/julssh.git
cd julssh
bash install.sh
```

### Desde .deb (Debian/Ubuntu amd64)

Descargá el `.deb` del último [release](https://github.com/felipendelicia/julssh/releases) e instalá:

```bash
sudo dpkg -i julssh_amd64.deb
```

Instala en `/usr/local/bin/julssh`.

---

Por defecto instala en `~/.local/bin/`. Para instalación global:

```bash
sudo bash install.sh /usr/local/bin
```
```

- [ ] **Step 2: Verificar que el README se ve bien**

```bash
cat README.md
```

Confirmar que la sección de instalación tiene las 3 opciones: curl oneliner, desde fuente, .deb.

- [ ] **Step 3: Commit y push**

```bash
git add README.md
git commit -m "docs: update install section with curl oneliner and .deb instructions"
git push
```

---

## Task 6: Primer release — tag v0.2.0

**Files:** ninguno (solo git)

- [ ] **Step 1: Verificar que todo está limpio**

```bash
git status
git log --oneline -5
go test ./...
```

Expected: working tree clean, tests pasan.

- [ ] **Step 2: Crear y pushear tag**

```bash
git tag v0.2.0
git push origin v0.2.0
```

- [ ] **Step 3: Verificar que el workflow se disparó**

Ir a `https://github.com/felipendelicia/julssh/actions` y confirmar que el job "Release" está corriendo o completó.

- [ ] **Step 4: Verificar el GitHub Release**

Ir a `https://github.com/felipendelicia/julssh/releases` y confirmar que existe `v0.2.0` con los assets:
- `julssh_linux_amd64.tar.gz`
- `julssh_linux_arm64.tar.gz`
- `julssh_amd64.deb`
- `checksums.txt`

- [ ] **Step 5: Probar instalación con --download**

```bash
bash install.sh --download
julssh --version
```

Expected: `julssh v0.2.0`

---

## Self-Review

**Spec coverage:**
- [x] GoReleaser config con builds linux amd64/arm64 — Task 2
- [x] .deb amd64 via nfpms — Task 2
- [x] GitHub Actions trigger en tag v*.*.* — Task 3
- [x] fetch-depth: 0 para changelog — Task 3
- [x] GITHUB_TOKEN automático — Task 3
- [x] --version en main.go inyectado por GoReleaser — Task 1
- [x] install.sh --download via GitHub API — Task 4
- [x] Detecta arquitectura amd64/arm64 automáticamente — Task 4
- [x] README con curl oneliner + .deb instructions — Task 5
- [x] Tag v0.2.0 para disparar el pipeline — Task 6

**Type consistency:** no hay tipos Go nuevos, solo config files y bash.
